"""The PyQt6 editor used to design an environment.

Import the piece you need directly - ``from starton.gui.editor import run`` -
rather than through this package, so the widget modules stay free to import
each other.
"""
