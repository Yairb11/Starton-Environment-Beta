"""Everything that happens on boot, once Windows runs ``launch_apps.bat``.

Import the runner directly - ``from starton.startup.runner import run`` - so
the modules in here stay free to import each other.
"""
